spring:
  ai:
    ollama:
      chat:
        options:
          model: llama3.1
      base-url: http://localhost:11434

  application:
    name: ChattyPro
  data:
    mongodb:
      username: navidul
      password: navidul
      host: localhost
      port: 27017
      database: chat_app
      authentication-database: admin
server:
  port: 8080